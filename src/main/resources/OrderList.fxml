<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<AnchorPane prefHeight="530.0" prefWidth="688.0"
            stylesheets="@css/styles.css"
            xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="controllers.OrderListController">

    <!-- ✅ Conteneur Principal -->
    <VBox layoutX="50" layoutY="20" prefWidth="650" spacing="15" alignment="CENTER">

        <!-- 🏷️ Titre Centré -->
        <Label text="📦 My Orders"
               style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-alignment: center;"/>
        <!-- 🔍 Barre de Recherche -->
        <TextField fx:id="searchField" promptText="Rechercher une commande..."
                   onKeyReleased="#searchOrders"
                   style="-fx-padding: 8px; -fx-font-size: 14px; -fx-border-color: #ddd; -fx-background-radius: 5px;"/>

        <!-- 📜 Liste des Commandes -->
        <ScrollPane fitToWidth="true" prefHeight="350" prefWidth="600" style="-fx-background-color: transparent;">
            <VBox fx:id="orderListContainer" spacing="10">
                <padding>
                    <Insets bottom="10" left="10" right="10" top="10"/>
                </padding>
            </VBox>

        </ScrollPane>
        <!-- ✅ Boutons "Reload" et "Close" en bas -->
        <HBox spacing="20" alignment="CENTER">
            <Button text="🔄 Recharger" onAction="#reloadOrders"
                    style="-fx-background-color: #8E44AD; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 8px 12px; -fx-background-radius: 5px;"/>

            <Button text="❌ Fermer" onAction="#closeWindow"
                    style="-fx-background-color: #E74C3C; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 8px 12px; -fx-background-radius: 5px;"/>
        </HBox>

    </VBox>

</AnchorPane>
